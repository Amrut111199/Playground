#include<iostream>

using namespace std;
int main()
{
  int r,l;
  cin>>r>>l;
  if (2*r<=l)
    cout<<"circle can be inside a square";
  else if(2*r>l)
    cout<<"circle cannot be inside a square";
 
 
  //Type your code here.
}